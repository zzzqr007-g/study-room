// Pomodoro defaults
export const DEFAULT_FOCUS_DURATION = 45 * 60; // 45 minutes in seconds
export const DEFAULT_BREAK_DURATION = 5 * 60; // 5 minutes in seconds

// Preset durations for quick selection (in minutes)
export const FOCUS_PRESETS = [25, 45, 60, 90];
export const BREAK_PRESETS = [5, 10, 15];

// Theme
export const DAY_START_HOUR = 6; // 6:00 AM -> switch to light
export const NIGHT_START_HOUR = 18; // 6:00 PM -> switch to dark

// Statistics
export const DEFAULT_DAILY_GOAL_MINUTES = 120;

// Sidebar
export const SIDEBAR_COLLAPSED_WIDTH = 64;
export const SIDEBAR_EXPANDED_WIDTH = 320;
